a = 'Nick'    #1) Создать переменную типа String
b = 5         #2) Создать переменную типа Integer
c = 4.6       #3) Создать переменную типа Float
d = bytes(2)  #4) Создать переменную типа Bytes
e = [1, 2, 3, 5, 'name']      #5) Создать переменную типа List
f = False                     # Создать переменную типа bool
g = (11, "firstname", 4.6)    #6) Создать переменную типа Tuple
h = {11, 11, 23, 47, "hello", "hello"}   #7) Создать переменную типа Set
i = frozenset({22, 22, 34, "hi", "hi"})  #8) Создать переменную типа Frozen set
m = {"age":30, "number":7}               #9) Создать переменную типа Dict
#10) Вывести в консоль все выше перечисленные переменные с добавлением типа данных.
aa = [a, b, c, d, e, f, g, h, i ,m]
for bb in aa:
    print(bb, type(bb))
#11) Создать 2 переменные String, создать переменную в которой сканкатенируете эти переменные. Вывести в консоль.
j = 'name'
k = 'Nikita'
l = j + ' ' + k
print(l)
#12) Вывести в одну строку переменные типа String и Integer используя “,” (Запятую)
print(a, b, sep=',')
#13) Вывести в одну строку переменные типа String и Integer используя “+” (Плюс)
print(a, b, sep='+')
